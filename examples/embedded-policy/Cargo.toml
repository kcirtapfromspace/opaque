[package]
name = "opaque-embedded-policy-example"
version = "0.1.0"
edition = "2024"
publish = false
license = "BUSL-1.1"

# Deliberately separate: this application is an external consumer, not another
# member of the Opaque product workspace.
[workspace]

[dependencies]
opaque-core = { path = "../../crates/opaque-core" }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
