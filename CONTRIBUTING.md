# Contributing to Opaque

Thank you for your interest in contributing to Opaque.

## Prerequisites

- Rust 1.80+ (edition 2024)
- macOS or Linux
- Git

## Getting Started

```bash
git clone https://github.com/kcirtapfromspace/opaque.git
cd opaque
cargo build --workspace
cargo test --workspace
```

## Development Workflow

### Code Quality

Before submitting a PR, ensure:

```bash
# All tests pass
cargo test --workspace

# No clippy warnings
cargo clippy --workspace -- -D warnings

# Code is formatted
cargo fmt --check
```

### Running Locally

```bash
# Build all binaries
cargo build --release

# Initialize config
./target/release/opaque init

# Start daemon
./target/release/opaqued

# In another terminal
./target/release/opaque ping
./target/release/opaque execute test.noop
```

## Pull Request Expectations

- **Tests**: Add or update tests for any new functionality
- **Clippy**: Zero warnings with `cargo clippy --workspace -- -D warnings`
- **Format**: Run `cargo fmt` before committing
- **Docs**: Update documentation if behavior changes
- **Scope**: Keep PRs focused on a single concern
- **Commits**: Use clear, descriptive commit messages

## Releasing

Versions are derived, not chosen. Every merge to `main` recomputes the next
version from the conventional-commit types since the last tag — `feat:` makes
it a minor, `fix:` a patch, a `!` or `BREAKING CHANGE:` a major — and keeps one
`chore(release): vX.Y.Z` pull request open containing:

- the workspace version in `Cargo.toml`, which every crate inherits, and `Cargo.lock`
- `CHANGELOG.md` with the curated `[Unreleased]` section stamped as the
  release and a fresh `[Unreleased]` above it

That PR updates itself as more work merges, so it always reflects everything
that would ship. Merging it is the decision to release. Then:

```sh
git tag vX.Y.Z && git push origin vX.Y.Z
```

which runs `release.yml`: four cross-compiled targets, codesigned and notarized
on macOS, cosign-signed, with release notes generated by git-cliff. The tag push
is deliberately manual — GitHub does not start workflows for events raised by
`GITHUB_TOKEN`, so a tag pushed by CI would sit there and publish nothing.

The same preparation runs locally, which is the way to check what a release
would look like before it exists:

```sh
scripts/release-prep.sh          # version from the merged commits
scripts/release-prep.sh 0.3.0    # or state it explicitly
```

It writes `Cargo.toml`, `Cargo.lock` and `CHANGELOG.md`, commits nothing, and
is a no-op when the version is already current.

**The macOS half needs signing credentials**: `APPLE_CERTIFICATE`,
`APPLE_CERTIFICATE_PASSWORD`, `APPLE_TEAM_ID`, `APPLE_ID`, and
`APPLE_ID_PASSWORD`. Without them the two darwin jobs fail, and since the
release job needs every build, nothing is published at all — check they exist
before tagging.

## The Site

The docs and the landing page are one MkDocs build:

```sh
uv venv && source .venv/bin/activate
uv pip install -r docs/requirements.txt
mkdocs serve            # http://127.0.0.1:8000
mkdocs build --strict   # what CI and both deploys run
```

`--strict` is not optional in a deploy: a dead internal link or an unknown
config key fails the build rather than shipping a broken site. Package versions
are pinned in `docs/requirements.txt` and Python in `.python-version`, so the
build resolves identically everywhere.

It publishes to two places:

| Target | Where | How |
|---|---|---|
| **opaque.info** (canonical) | Cloudflare Pages | Builds from `main`; output directory comes from `wrangler.toml`, build command from the Pages dashboard |
| kcirtapfromspace.github.io/opaque | GitHub Pages | `.github/workflows/pages.yml` |

Canonical URLs and the sitemap point at `opaque.info` (`site_url` in
`mkdocs.yml`), so the mirror never competes with it in search results. Asset
paths are relative, so one build serves correctly both at a domain root and
under a subpath.

Brand decisions — the slogan, the redaction motif, palette, type, and voice —
live in [BRAND.md](BRAND.md). Read it before changing the landing page.

## Architecture

See [Architecture](docs/architecture.md) for an overview. Key principles:

- **Deny-by-default**: Nothing is permitted without an explicit policy rule
- **Fail closed**: Errors and unavailable backends result in denial, never in secret disclosure
- **Typestate sanitization**: Response types enforce that secrets are scrubbed before reaching clients
- **Audit everything**: Every operation request, policy decision, and approval is logged

## Code of Conduct

This project follows the [Contributor Covenant Code of Conduct](CODE_OF_CONDUCT.md).
